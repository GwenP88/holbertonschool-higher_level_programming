-- Script that lists all records of the table second_table of the database hbtn_0c_0 in your MySQL server ( should display both the score and the name (in this order) and be ordered by score (top first))
-- Lists all records of the table second_table.
SELECT score, name FROM first_table ORDER BY score DESC;
